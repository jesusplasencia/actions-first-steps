name: Deploy to server

on:
  push:
    branches: ["main"]

jobs:
  first_job:
    runs-on: ubuntu-latest
    env:
      NAME: "1492"
    steps:
      - name: First Job
        run:  echo "Hello from $NAME"

  second_job:
    if: ${{ jobs.first_job.result == 'success' }}
    # needs: first_job
    runs-on: ubuntu-latest
    steps:
      - name: Second Job
        run:  echo "Second Jobber..."
