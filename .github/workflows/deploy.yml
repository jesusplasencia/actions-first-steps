name: Deploy to server

on:
  push:
    branches: ["main"]

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: First Job
        run:  echo "Hello from first jobby..."

  second_job:
    needs: first_job
    runs-on: ubuntu-latest
    steps:
      - name: Second Job
        run:  echo "Second Jobber..."
